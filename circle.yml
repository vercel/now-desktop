machine:
  environment:
    XCODE_SCHEME: symbolicator
    XCODE_WORKSPACE: symbolicator
  xcode:
    version: 8.2
  node:
    version: 6.1.0
deployment:
  release:
    branch: master
    commands:
      - npm run dist
