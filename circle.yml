machine:
  xcode:
    version: 8.2

deployment:
  artifacts:
    branch: /^(?!master$).*$/
    owner: zeit
    commands:
      - yarn build
      - cp out/*.zip $CIRCLE_ARTIFACTS
  release:
    tag: /.*/
    commands:
      - yarn build
