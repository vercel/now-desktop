machine:
  xcode:
    version: 8.2

deployment:
  artifacts:
    branch: /^(?!master$).*$/
    owner: zeit
    commands:
      - npm run build
      - cp out/*.zip $CIRCLE_ARTIFACTS
  release:
    tag: /.*/
    commands:
      - npm run build
